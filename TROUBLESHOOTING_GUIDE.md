# 🔧 دليل استكشاف الأخطاء - Troubleshooting Guide

## 🚨 المشاكل الشائعة وحلولها

### 1. **مشاكل التثبيت**

#### ❌ **خطأ: "ModuleNotFoundError: No module named 'cv2'"**
```bash
# الحل:
pip uninstall opencv-python
pip install opencv-python-headless==4.8.1.78
```

#### ❌ **خطأ: "Could not find a version that satisfies torch"**
```bash
# الحل:
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
```

#### ❌ **خطأ: "Microsoft Visual C++ 14.0 is required"**
```bash
# الحل:
# حمل وثبت: Microsoft C++ Build Tools
# أو استخدم:
pip install --only-binary=all opencv-python
```

---

### 2. **مشاكل تشغيل النظام**

#### ❌ **خطأ: "Port 8080 is already in use"**
```bash
# الحل 1: استخدم منفذ مختلف
python run_web_app.py --port 8081

# الحل 2: أوقف العملية المستخدمة للمنفذ
# Windows:
netstat -ano | findstr :8080
taskkill /PID [PID_NUMBER] /F

# Linux:
sudo lsof -ti:8080 | xargs sudo kill -9
```

#### ❌ **خطأ: "Permission denied"**
```bash
# الحل:
# تأكد من تشغيل Terminal كـ Administrator
# أو غير المجلد:
chmod -R 755 employee-monitoring-ai/
```

#### ❌ **خطأ: "CUDA not available"**
```
✅ هذا عادي! النظام يعمل على CPU بشكل طبيعي
لا حاجة لإصلاح - النظام محسن للعمل بدون GPU
```

---

### 3. **مشاكل الأداء**

#### 🐌 **النظام بطيء جداً**
```json
// الحل: قلل الإعدادات في config.json
{
  "yolo": {
    "model": "yolov8n.pt",
    "imgsz": 320,
    "confidence": 0.4
  },
  "level2_processor": {
    "frame_skip": 5,
    "max_duration": 60,
    "enable_advanced_ai": false
  }
}
```

#### 💾 **نفاد الذاكرة: "Out of Memory"**
```bash
# الحل 1: قلل حجم الصورة
"imgsz": 320

# الحل 2: زد تخطي الإطارات
"frame_skip": 5

# الحل 3: قلل مدة الفيديو
"max_duration": 60

# الحل 4: أغلق برامج أخرى
```

#### 🔥 **المعالج ساخن جداً**
```json
// الحل: قلل الحمل
{
  "level2_processor": {
    "frame_skip": 10,
    "enable_advanced_ai": false,
    "activity_window": 5
  }
}
```

---

### 4. **مشاكل الفيديو**

#### ❌ **خطأ: "Could not read video file"**
```bash
# الأسباب المحتملة:
1. الفيديو تالف
2. صيغة غير مدعومة
3. الملف مشفر

# الحلول:
# 1. جرب فيديو آخر
# 2. حول الفيديو لـ MP4:
ffmpeg -i input.avi output.mp4

# 3. تحقق من الصيغ المدعومة:
# MP4, AVI, MOV, MKV, WMV
```

#### ❌ **خطأ: "Video processing failed"**
```bash
# تحقق من:
1. حجم الفيديو (< 500 MB)
2. مدة الفيديو (< 10 دقائق)
3. دقة الفيديو (< 4K)
4. مساحة التخزين الفارغة

# الحل:
# قلل جودة الفيديو أو اقطعه لأجزاء أصغر
```

#### 📹 **لا تظهر نتائج للفيديو**
```bash
# الأسباب:
1. لا يوجد أشخاص في الفيديو
2. الأشخاص صغار جداً
3. إضاءة ضعيفة
4. حركة سريعة

# الحل:
# قلل confidence في config.json:
"confidence": 0.25
```

---

### 5. **مشاكل التعرف على الوجوه**

#### 👤 **لا يتعرف على الموظفين**
```bash
# تحقق من:
1. وجود صور في data/employees/
2. جودة الصور (واضحة، مضيئة)
3. عدد الصور (5-10 لكل شخص)

# الحل:
python train_faces.py
```

#### 😕 **تعرف خاطئ على الأشخاص**
```bash
# الحل:
1. أضف صور أكثر للشخص الصحيح
2. احذف الصور الضبابية
3. أعد التدريب:
python train_faces.py
```

#### 🔍 **دقة التعرف منخفضة**
```json
// قلل العتبة في config.json:
{
  "face_recognition": {
    "similarity_threshold": 0.5
  }
}
```

---

### 6. **مشاكل الواجهة**

#### 🌐 **لا تفتح الواجهة**
```bash
# تحقق من:
1. تشغيل النظام بنجاح
2. عدم وجود أخطاء في Terminal
3. المنفذ الصحيح

# جرب:
http://localhost:8080
http://127.0.0.1:8080
http://[your-ip]:8080
```

#### 📱 **الواجهة بطيئة**
```bash
# الأسباب:
1. معالجة فيديو في الخلفية
2. ملفات كبيرة
3. ذاكرة ممتلئة

# الحل:
# انتظر انتهاء المعالجة أو أعد تشغيل النظام
```

#### 🖼️ **لا تظهر الصور**
```bash
# تحقق من:
1. وجود مجلد snapshots/
2. صلاحيات الكتابة
3. مساحة التخزين

# الحل:
mkdir -p web_app/static/uploads/test_videos/snapshots
chmod 755 web_app/static/uploads/
```

---

### 7. **مشاكل الذكاء الاصطناعي**

#### 🧠 **لا تظهر رؤى الذكاء الاصطناعي**
```json
// تأكد من التفعيل في config.json:
{
  "level2_processor": {
    "enable_advanced_ai": true
  }
}
```

#### ⚠️ **تنبيهات خاطئة للشذوذ**
```json
// ارفع العتبة:
{
  "ai_thresholds": {
    "anomaly_threshold": 0.8,
    "risk_threshold": 0.7
  }
}
```

#### 📊 **نتائج غير دقيقة**
```bash
# الحل:
1. اتركه يعمل لفترة أطول (يتعلم مع الوقت)
2. أضف بيانات تدريب أكثر
3. اضبط العتبات حسب بيئة العمل
```

---

### 8. **مشاكل النظام**

#### 💻 **استهلاك عالي للموارد**
```bash
# مراقبة الاستهلاك:
# Windows:
tasklist | findstr python

# Linux:
top | grep python

# الحل:
# قلل الإعدادات أو أغلق برامج أخرى
```

#### 📝 **ملفات السجل كبيرة**
```bash
# تنظيف السجلات:
# Windows:
del logs\*.log

# Linux:
rm logs/*.log

# أو قلل مستوى السجل:
"log_level": "WARNING"
```

#### 🗄️ **امتلاء التخزين**
```bash
# تنظيف الملفات القديمة:
# احذف الفيديوهات المعالجة القديمة
# احذف الصور القديمة
# فعّل التنظيف التلقائي:
"auto_cleanup": true
```

---

## 🔍 أدوات التشخيص

### **1. فحص النظام:**
```bash
python -c "
import sys
print('Python:', sys.version)
import cv2
print('OpenCV:', cv2.__version__)
import torch
print('PyTorch:', torch.__version__)
print('CUDA Available:', torch.cuda.is_available())
"
```

### **2. اختبار المكونات:**
```bash
# اختبار سريع
python quick_test.py

# اختبار شامل
python test_processor.py

# اختبار الواجهة
curl http://localhost:8080
```

### **3. مراجعة السجلات:**
```bash
# آخر 50 سطر
tail -50 logs/app.log

# البحث عن أخطاء
grep "ERROR" logs/app.log

# البحث عن تحذيرات
grep "WARNING" logs/app.log
```

---

## 📞 طلب المساعدة

### **معلومات مطلوبة عند طلب المساعدة:**

1. **نظام التشغيل:**
   - Windows 10/11
   - Ubuntu 20.04
   - macOS Big Sur

2. **مواصفات الجهاز:**
   - المعالج (CPU)
   - الذاكرة (RAM)
   - كرت الشاشة (GPU)

3. **إصدار Python:**
   ```bash
   python --version
   ```

4. **رسالة الخطأ الكاملة:**
   - انسخ النص كاملاً
   - أرفق لقطة شاشة

5. **خطوات إعادة المشكلة:**
   - ماذا فعلت؟
   - ماذا توقعت؟
   - ماذا حدث فعلاً؟

6. **ملفات السجل:**
   ```bash
   # أرفق آخر 100 سطر من السجل
   tail -100 logs/app.log
   ```

---

## ✅ قائمة التحقق السريعة

عند مواجهة مشكلة، تحقق من:

- [ ] تفعيل البيئة الافتراضية
- [ ] تثبيت جميع المكتبات
- [ ] صحة ملف config.json
- [ ] وجود مساحة تخزين كافية
- [ ] عدم تشغيل عمليات أخرى على نفس المنفذ
- [ ] صلاحيات الكتابة في المجلدات
- [ ] اتصال الإنترنت (لأول مرة)

---

## 🎯 نصائح لتجنب المشاكل

### **الوقاية خير من العلاج:**

1. **استخدم بيئة افتراضية دائماً**
2. **احتفظ بنسخة احتياطية من الإعدادات**
3. **راقب استهلاك الموارد**
4. **نظف الملفات القديمة دورياً**
5. **حدّث النظام بانتظام**
6. **اختبر التغييرات على بيانات صغيرة أولاً**

### **للاستخدام المستقر:**

1. **لا تغير الإعدادات أثناء المعالجة**
2. **أغلق البرامج غير الضرورية**
3. **استخدم فيديوهات بجودة معقولة**
4. **راجع السجلات دورياً**

---

## 🎉 مبروك!

**الآن لديك دليل شامل لحل أي مشكلة! 🛠️**

تذكر:
- ✅ معظم المشاكل لها حلول بسيطة
- ✅ السجلات تحتوي على معلومات مفيدة
- ✅ الإعدادات قابلة للتخصيص
- ✅ النظام مصمم ليكون مستقراً

**لا تتردد في طلب المساعدة! 💪**
